import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("PhotoX")
            .font(.title)
            .frame(minWidth: 800, minHeight: 600)
    }
}

#Preview {
    ContentView()
}
